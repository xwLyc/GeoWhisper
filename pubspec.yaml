name: geo_whisper
description: 周边树洞 App MVP
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^9.0.2 # 定位核心插件
  permission_handler: ^10.2.0 # 权限管理
  emoji_picker_flutter: ^2.0.12 #表情
  cupertino_icons: ^1.0.2
  json_annotation: ^4.8.0 # ✅ 运行时依赖
  web_socket_channel: ^2.4.0
  dash_chat_2: ^0.0.20 
  shared_preferences: ^2.5.3

  
  # flutter_baidu_mapapi_map: ^3.5.0
  # flutter_bmflocation: ^3.6.1
  # geocoding: ^2.1.0 #逆地理编码
  # flutter_baidu_yingyan_trace: ^2.2.0 # 百度逆地理编码

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3   # ✅ 代码生成工具
  json_serializable: ^6.7.1 # ✅ 代码生成插件

flutter:
  uses-material-design: true
  assets:
    - assets/